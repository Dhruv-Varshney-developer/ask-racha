# Discord Bot Configuration
# Required: Discord bot token from Discord Developer Portal
DISCORD_TOKEN=your_discord_bot_token_here

# Optional: AskRacha API Configuration
# URL of the AskRacha RAG API (default: http://localhost:5000)
ASKRACHA_API_URL=http://localhost:5000

# Optional: API Request Configuration
# Timeout for API requests in seconds (default: 10)
API_TIMEOUT=10

# Optional: Discord Message Configuration
# Maximum length for Discord responses (default: 2000)
MAX_RESPONSE_LENGTH=2000

# Optional: Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# Optional: Retry Configuration
# Number of retry attempts for failed API requests (default: 3)
RETRY_ATTEMPTS=3

# Delay between retry attempts in seconds (default: 1.0)
RETRY_DELAY=1.0